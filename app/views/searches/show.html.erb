<%=
  react_component('SpatialSearch',
    bounds: @bounds,
    pitch_type: @pitch_type,
    start: @start,
    end: @end,
    guests: @guests,
    amenities: @amenities,
    pitch_types: @pitch_types,
    amenity_types: @amenity_types,
    page: @page
  )
%>
